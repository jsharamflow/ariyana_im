# Private deployment — encrypted static memoranda
Content is AES-256 encrypted at rest (StatiCrypt, PBKDF2-derived keys). Paths are unlisted random slugs. Root serves nothing. Do not add a sitemap or links to the slugs.
